se ingresa a la vista productos con el link http://localhost:8080/api/products
se ingresa a la vista de carrito con el link http://localhost:8080/api/cart
se ingresa al chat con el link http://localhost:8080/loginChat

se ingresa al login con el link http://localhost:8080/login
se ingersa al register con el link http://localhost:8080/register
al al logearse se crea automaticamente el carrito 


el carrito se crea en la vista de carrito y se obtiene el id del carrito 
se puede cargar el carrito desde la vista carrito con el id del producto o haciendo clic el la lista de productos que se encuentra en la vista  de productos 
al hacer click en la lista de productos se puede va a vista detallada y ahi con el id del carrito se agrega al mismo 
destro de la vista de carrito con el id del carrito se puede ir a la vista deteellada del carrito 

